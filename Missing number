//without using sorting extra space.
//as we that sum of natural number is N*(N+1)/2 so by apply this logic we can solve the problem
int missingNumber(int A[], int N)
{
    
    int sum = (N*(N+1))/2;
    for(int i=0;i<N-1;i++) sum -= A[i];
    return sum;
    
}
